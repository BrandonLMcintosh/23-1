{% extends "root/base.html.j2" %}

{% block title %}
    
{% endblock title %}

{% block body %}
    <div class="container">
        <h2>{{post.title}}</h2>
        <sub>Created {{post.created_at}} by <a href="/users/{{user_id}}">user.full_name</a></sub>
        {% if post.updated_at %}
            <sub>{{post.updated_at}}</sub>
        {% endif %}
        
        <p>{{post.content}}</p>
    </div>
{% endblock body %}